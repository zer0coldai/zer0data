remote:
  host: cryptodata
  data_dir: /root/zer0data/data/download

local:
  data_dir: /data/zer0data/download
  state_dir: /data/zer0data/download/_ingested
  log_dir: /data/zer0data/logs
  project_root: /Users/rock/work/zer0data

storage:
  type: r2               # r2 或 rsync
  r2:
    bucket: zer0data
    prefix: download      # bucket 内路径前缀
    transfers: 8          # rclone 并行传输数

download:
  symbols:
    - BTCUSDT
    - ETHUSDT
    - SOLUSDT
    - BNBUSDT
  intervals:
    - 1m
    - 5m
    - 15m
    - 1h
    - 4h
    - 1d
  market: um

clickhouse:
  host: localhost
  port: 8123
  database: zer0data
  username: default
  password: ""

schedule:
  remote_time: "01:30"
  local_time: "10:00"
